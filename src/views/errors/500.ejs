<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title><%= status %> - Error</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet"/>
  <link href="https://cdn.jsdelivr.net/npm/remixicon/fonts/remixicon.css" rel="stylesheet"/>
  <style>
    body{font-family:Inter,sans-serif;background:#fff;color:#111;margin:0}
    .wrap{min-height:100vh;display:flex;align-items:center;justify-content:center;padding:24px}
    .card{border:1px solid #e5e7eb;border-radius:16px;padding:22px;max-width:520px;width:100%;
      box-shadow:0 8px 30px rgba(0,0,0,.06)}
    .h{font-size:26px;font-weight:800;margin:0 0 8px}
    .p{color:#6b7280;margin:0 0 18px;white-space:pre-wrap}
    a{display:inline-flex;gap:8px;align-items:center;padding:10px 14px;border-radius:10px;border:1px solid #e5e7eb;text-decoration:none;color:#111}
    a:hover{background:#111;color:#fff}
  </style>
</head>
<body>
  <div class="wrap">
    <div class="card">
      <div class="h"><i class="ri-bug-line"></i> <%= status %> — Terjadi kesalahan</div>
      <p class="p"><%= message %></p>
      <a href="/"><i class="ri-home-4-line"></i> Kembali ke Home</a>
    </div>
  </div>
</body>
</html>
